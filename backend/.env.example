# Database
MONGODB_URI=mongodb://localhost:27017/email-marketing-saas

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRATION=7d

# Server
PORT=3000
NODE_ENV=development
# URL base do app (opcional). Se definida, imagens no HTML gerado usam URLs absolutas (ex: http://localhost:3000).
# APP_URL=http://localhost:3000

# Upload
MAX_FILE_SIZE=10485760
UPLOAD_DEST=./uploads

# OCR
TESSERACT_LANG=por+eng

# CORS
CORS_ORIGIN=http://localhost:5173

# IA open-source (estilo OpenClaw: self-hosted, dados sob seu controle)
# Opção 1: Ollama (local, gratuito) — texto: ollama pull llama3.2 | visão (analisar imagem): ollama pull llava
AI_ENABLED=true
AI_PROVIDER=ollama
OLLAMA_BASE_URL=http://localhost:11434
AI_MODEL=llama3.2
# Modelo de visão para "Analisar imagem com IA" (Ollama: llava, gemma3, etc.)
AI_VISION_MODEL=llava

# Opção 2: OpenAI (ou API compatível)
# AI_ENABLED=true
# AI_PROVIDER=openai
# OPENAI_API_KEY=sk-...
# OPENAI_BASE_URL=https://api.openai.com/v1
# AI_MODEL=gpt-4o-mini
